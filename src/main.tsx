import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import UpdateGrid from './UpdateGrid.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <UpdateGrid/>
  </React.StrictMode>,
)
